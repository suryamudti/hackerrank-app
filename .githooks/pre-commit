#!/bin/sh
echo "Running pre-commit quality checks..."
./gradlew lintDebug testDebugUnitTest --daemon
if [ $? -ne 0 ]; then
  echo "Quality check failed (lint or tests failed). Aborting commit."
  exit 1
fi
echo "Quality check passed!"
exit 0
